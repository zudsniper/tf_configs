exec overrides/reset.cfg
exec overrides/sillybinds.cfg

alias +sex "slot2; +attack;"
alias -sex "-attack"

alias +prim "slot1; +attack;"
alias -prim "-attack"

bind mouse1 "+sex; spec_next"
bind mouse2 "+prim; spec_prev"

alias +charge "+attack2; slot2; dropitem; voicemenu 2 4; spec_prev"
alias -charge "-attack2; say_team pop"

alias fakeuber "voicemenu 1 7; say_team fake"
alias maskuber "voicemenu 2 4; say_team red e"

bind mouse4 +charge
//bind mouse3 maskuber
//bind mouse4 fakeuber